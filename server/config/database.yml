default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  encoding: unicode
  database: dgidb
  host: localhost

test:
  <<: *default
  encoding: unicode
  database: dgidb_test
  host: localhost
