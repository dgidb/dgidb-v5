commands:
    01_update:
        command: "sudo yum update"
    02_update_postgres:
        command: "sudo amazon-linux-extras install postgresql13"

container_commands:
    01_make_pids_dir:
        command: "mkdir -p tmp/pids"
    02_set_permissions:
        command: "sudo chmod -R 777 tmp/pids"
